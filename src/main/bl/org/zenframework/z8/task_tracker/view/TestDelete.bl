import org.zenframework.z8.task_tracker.model.CompletedStats;

[entry]
[request true]
[displayName "Статистика выполненных задач"]
public class TestDelete extends CompletedStats {

	columns = {
		name,
		firstName,
		lastName,
		completedCount
	};

}